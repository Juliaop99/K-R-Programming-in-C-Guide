#include <stdio.h>
//Point 1.1
//This first line tells the computer to include information about the basic input/output library

int main() {	//Every program must have a main function, because it starts executing from there
    //This function has not received any argument value '(char main(int patata))'
    printf("Hello world! \n");
    //We call library function printf to print the sequence of characters introduced
}

//The statements of a function are enclosed in brackets {}